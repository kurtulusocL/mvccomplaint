@using PagedList
@using PagedList.Mvc
@model IPagedList<Complain.Entities.Entities.Contact>
@{
    ViewBag.Title = "İletişimden Gelen Mesajlar";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<section id="container" class="">
    <section id="main-content">
        <section class="wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <section class="panel">
                        <header class="panel-heading">
                            @ViewBag.Title
                        </header>
                        <table class="table table-striped table-advance table-hover">
                            <thead>
                                <tr>
                                    <th class="hidden-phone"><i class="icon-question-sign"></i> Adı-Soyadı</th>
                                    <th><i class="icon-bookmark"></i> Mail Adresi</th>
                                    <th><i class="icon-bookmark"></i> Mesaj Konusu</th>
                                    <th><i class="icon-time"></i> Eklenme Tarihi</th>
                                    <th>İşlemler</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.NameSurname</td>
                                        <td class="hidden-phone">@item.MailAddress</td>
                                        <td>@item.Subject</td>
                                        <td>@item.CreatedTime.ToShortDateString()</td>
                                        <td>
                                            <a href="~/Contact/ContacttDetail/@item.Id"><button class="btn btn-info btn-xs"><i class="icon-ok">Detay</i></button></a>
                                            <a href="~/Contact/Delete/@item.Id"><button class="btn btn-danger btn-xs"><i class="icon-trash ">Sil</i></button></a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </section>
                </div>
            </div>
            <div class="pagination" align="center">
                <div class="pagination-large">
                    @Html.PagedListPager(Model, Page => Url.Action("yonas", new { Page }), PagedListRenderOptions.ClassicPlusFirstAndLast)
                </div>
            </div>
        </section>
    </section>
</section>
