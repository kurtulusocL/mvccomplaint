@model IEnumerable<Complain.Entities.Entities.About>
@{
    ViewBag.Title = "Hakkımızda Admin Sayfası";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<section id="container" class="">
    <section id="main-content">
        <section class="wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <section class="panel">
                        <header class="panel-heading">
                            @ViewBag.Title || <a href="~/About/Create/" class="btn btn-primary">Yeni Hakkımızda Kısmı Ekle</a>
                        </header>
                        <table class="table table-striped table-advance table-hover">
                            <thead>
                                <tr>
                                    <th width="80"><i class="icon-bullhorn"></i> Resim</th>
                                    <th class="hidden-phone"><i class="icon-question-sign"></i> Başlık</th>
                                    <th><i class="icon-bookmark"></i> Altbaşlık</th>
                                    <th><i class="icon-time"></i> Eklenme Tarihi</th>
                                    <th>İşlemler</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td width="80"><img src="~/img/@item.Photo" class="img-responsive" /></td>
                                        <td class="hidden-phone">@item.Title</td>
                                        <td>@item.Subtitle</td>
                                        <td>@item.CreatedTime.ToShortDateString()</td>
                                        <td>
                                            <a href="~/About/AboutDetail/@item.Id"><button class="btn btn-primary btn-xs"><i class="icon-pencil">Detay</i></button></a>
                                            <a href="~/About/Edit/@item.Id"><button class="btn btn-success btn-xs"><i class="icon-pencil">Güncelle</i></button></a>
                                            <a href="~/About/Delete/@item.Id"><button class="btn btn-danger btn-xs"><i class="icon-trash ">Sil</i></button></a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </section>
                </div>
            </div>
        </section>
    </section>
</section>