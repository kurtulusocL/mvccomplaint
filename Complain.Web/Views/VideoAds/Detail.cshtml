@model Complain.Entities.Entities.VideoAds
@{
    ViewBag.Title = "Video Reklam Detay Bilgileri";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<section id="container" class="">
    <section id="main-content">
        <section class="wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <section class="panel">
                        <header class="panel-heading">
                            @ViewBag.Title  || <a href="~/VideoAds/yonas" class="btn btn-primary">Video Reklam Admin Sayfasına Dön</a> || <a href="~/VideoAds/Delete/@Model.Id" class="btn btn-danger">Video Reklamı Sil</a>
                        </header>
                        <table class="table table-striped table-advance table-hover">
                            <thead>
                                <tr>
                                    <th><i class="icon-bullhorn"></i>İnternet Sitesi</th>
                                    <th class="hidden-phone"><i class="icon-question-sign"></i>Video Linki</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td>@Model.WebSite</td>
                                    <td>@Model.VideoLink</td>
                                </tr>
                            </tbody>
                        </table>
                    </section>
                </div>
            </div>
        </section>
    </section>
</section>
